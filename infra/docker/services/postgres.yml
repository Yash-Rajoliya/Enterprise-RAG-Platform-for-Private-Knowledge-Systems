version: "3"

services:
  postgres:
    image: postgres:16

    environment:
      POSTGRES_USER: rag
      POSTGRES_PASSWORD: rag
      POSTGRES_DB: enterprise_rag

    ports:
      - "5432:5432"