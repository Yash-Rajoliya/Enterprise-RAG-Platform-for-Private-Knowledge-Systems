global:
  scrape_interval: 15s

scrape_configs:

  - job_name: api-server

    static_configs:
      - targets:
          - api-server:8000

  - job_name: retrieval-service

    static_configs:
      - targets:
          - retrieval-service:8002